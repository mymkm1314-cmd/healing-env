server:
 port: 8081

spring:
 datasource:
  url: jdbc:mysql://localhost:3306/yllypj?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
  username: root
  password: 123456
  driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
 configuration:
  map-underscore-to-camel-case: true
  log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
 global-config:
  db-config:
   logic-delete-field: deleted
   logic-delete-value: 1
   logic-not-delete-value: 0
   id-type: auto
 mapper-locations: classpath:mapper/*.xml

# JWT 配置
jwt:
  secret: medical-healing-env-secret-key-2026
  expiration: 86400000  # 24小时